<h2>User Settings</h2>

<!-- For FILE UPLOADS - method="POST" & enctype="multipart/form-data" -->
<form action="/process-settings" method="POST" enctype="multipart/form-data">
  {{#if currentUser.avatar}}
    <img src="{{currentUser.avatar}}" />
  {{/if}}

  <label>
    Avatar:
    <input type="file" name="avatarUpload" />
  </label>

  <label>
    Full name:
    <input type="text" name="fullName" value="{{currentUser.fullName}}"
        placeholder="Leia Organa" />
  </label>

  <label>
    Email:
    <input type="email" name="email" value="{{currentUser.email}}"
        placeholder="leia@resistance.com" />
  </label>

  <button>Save Settings</button>
</form>
